#include <iostream>

int oneTen()
{
    // type identifier { expression };
    int x{ 2 + 3 }; // 2 + 3 is an expression that has no semicolon -- the semicolon is at the end of the statement containing the expression
    return 0;
}

// statements are used when we want the program to perform an action whereas expressions are used when we want the program to calculate a value
